# Руководство по типу "message"

## 📋 Как работает тип "message"

### Новая логика (текущая версия):

1. **При нажатии на кнопку с типом `message`**:
   - Отправляется **новое текстовое сообщение** с текстом из столбца "Текст"
   - Сообщение **не редактируется**, а отправляется заново

2. **Привязка кнопок к сообщению**:
   - Кнопки (mess_button или reply_button) привязываются к message через **parent_id**
   - В столбце "Родитель ID" указывается ID сообщения (message)
   - Кнопки отображаются вместе с текстовым сообщением

## 📝 Пример структуры в таблице

### Пример 1: Простое сообщение с кнопками

| ID | Название | Родитель ID | Тип | Текст | Порядок |
|----|----------|-------------|-----|-------|---------|
| 1 | О нас | | message | Мы лучшая компания... | 1 |
| 2 | Контакты | 1 | mess_button | | 1 |
| 3 | Услуги | 1 | mess_button | | 2 |

**Результат:**
- При нажатии на "О нас" отправляется сообщение с текстом "Мы лучшая компания..."
- Под сообщением появляются кнопки: "Контакты" и "Услуги"

### Пример 2: Сообщение с reply кнопками

| ID | Название | Родитель ID | Тип | Текст | Порядок |
|----|----------|-------------|-----|-------|---------|
| 10 | Помощь | | message | Нужна помощь? Обратитесь к нам... | 1 |
| 11 | Связаться | 10 | reply_button | | 1 |
| 12 | FAQ | 10 | reply_button | | 2 |

**Результат:**
- При нажатии на "Помощь" отправляется сообщение с текстом
- Внизу экрана появляются reply кнопки: "Связаться" и "FAQ"

### Пример 3: Комбинированный вариант

| ID | Название | Родитель ID | Тип | Текст | Порядок |
|----|----------|-------------|-----|-------|---------|
| 20 | Информация | | message | Добро пожаловать... | 1 |
| 21 | Услуги | 20 | mess_button | | 1 |
| 22 | Контакты | 20 | mess_button | | 2 |
| 23 | Главное меню | 20 | reply_button | | 1 |

**Результат:**
- При нажатии отправляется сообщение с текстом
- Под сообщением: inline кнопки "Услуги" и "Контакты"
- Внизу экрана: reply кнопка "Главное меню"

## 🔄 Отличия от старой версии

### Старая версия:
- `message` редактировал существующее сообщение
- Кнопки привязывались через parent_id к категориям

### Новая версия:
- `message` отправляет **новое сообщение**
- Кнопки привязываются к `message` через **parent_id = ID сообщения**

## 💡 Преимущества новой логики

✅ **Чистота диалога** - каждое сообщение отправляется отдельно  
✅ **Гибкость** - можно привязать любые кнопки к любому сообщению  
✅ **Понятность** - логика привязки через parent_id более очевидна  
✅ **Масштабируемость** - легко добавлять новые кнопки к существующим сообщениям  

## ⚙️ Технические детали

### Обработка message:
1. При нажатии на кнопку с типом `message` вызывается `callback.message.answer()`
2. Отправляется новое сообщение с текстом из столбца "Текст"
3. К сообщению прикрепляются кнопки, у которых `parent_id = ID этого message`

### Поиск дочерних элементов:
- Система ищет все элементы, у которых `parent_id` совпадает с ID сообщения
- Эти элементы отображаются как кнопки под/внизу сообщения

## 📋 Рекомендации

1. **Используйте message для:**
   - Информационных сообщений
   - Текстовых блоков с привязанными действиями
   - Сообщений, к которым нужно привязать несколько кнопок

2. **Используйте mess_button для:**
   - Навигации по категориям
   - Открытия подменю
   - Элементов без текстового содержимого

3. **Используйте reply_button для:**
   - Быстрого доступа к основным функциям
   - Кнопок, которые должны быть всегда видны

## ⚠️ Важно

- **ID сообщения должен быть уникальным** во всей таблице
- **parent_id кнопок должен точно совпадать** с ID сообщения
- Если parent_id не найден, кнопка не будет привязана к сообщению
