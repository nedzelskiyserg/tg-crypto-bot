# ✅ Настройка Google Sheets завершена!

## 📋 Что уже сделано:

1. ✅ `.env` файл настроен с правильными параметрами
2. ✅ Скрипты для проверки и настройки созданы
3. ✅ Все зависимости указаны в `requirements.txt`

## 🔧 Что нужно сделать ВАМ:

### Шаг 1: Получите credentials.json

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Выберите проект: **fluted-bit-439519-k2**
3. Перейдите: **APIs & Services** → **Credentials**
4. Найдите Service Account: **telegram-bot-cms**
5. Нажмите на него
6. Перейдите на вкладку **Keys**
7. Нажмите **Add Key** → **Create new key**
8. Выберите формат **JSON**
9. Нажмите **Create**
10. Сохраните скачанный файл как **`credentials.json`** в корень проекта (рядом с `main.py`)

### Шаг 2: Предоставьте доступ к таблице

1. Откройте вашу таблицу:
   https://docs.google.com/spreadsheets/d/1OVEn5gfgvngL8nBk5iYAHk8AtQN5FgYA4Hxp_l_FKdo/edit

2. Нажмите кнопку **"Share"** (Поделиться) в правом верхнем углу

3. В поле "Add people and groups" вставьте email:
   ```
   telegram-bot-cms@fluted-bit-439519-k2.iam.gserviceaccount.com
   ```

4. Установите права доступа: **Editor** (Редактор)

5. Снимите галочку "Notify people" (чтобы не отправлять уведомление)

6. Нажмите **"Share"**

### Шаг 3: Установите зависимости

```bash
pip3 install -r requirements.txt
```

### Шаг 4: Проверьте настройки

```bash
python3 check_setup.py
```

Этот скрипт проверит:
- ✅ Настройки в .env
- ✅ Наличие credentials.json
- ✅ Установленные зависимости
- ✅ Подключение к Google Sheets

### Шаг 5: Запустите бота

```bash
python3 main.py
```

## 📝 Текущие настройки:

- **USE_GOOGLE_SHEETS**: `true`
- **GOOGLE_SHEETS_ID**: `1OVEn5gfgvngL8nBk5iYAHk8AtQN5FgYA4Hxp_l_FKdo`
- **Service Account Email**: `telegram-bot-cms@fluted-bit-439519-k2.iam.gserviceaccount.com`
- **Credentials Path**: `credentials.json`

## ⚠️ Важно:

1. **Не коммитьте `credentials.json` в Git** (уже добавлен в `.gitignore`)
2. **Храните `credentials.json` в безопасности**
3. **Если файл скомпрометирован**, удалите Service Account и создайте новый

## 🚀 После настройки:

Меню будет автоматически загружаться из Google Sheets. Вы можете редактировать таблицу онлайн, и изменения применятся после перезапуска бота.

## 📞 Если что-то не работает:

1. Запустите проверку: `python3 check_setup.py`
2. Проверьте логи бота на наличие ошибок
3. Убедитесь, что:
   - Google Sheets API включен
   - Google Drive API включен
   - Service Account имеет доступ к таблице
   - Таблица существует и доступна
