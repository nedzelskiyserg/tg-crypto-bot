# Руководство по типам кнопок

## 📋 Доступные типы кнопок

### 1. `mess_button` (бывший `button`)
**Описание:** Кнопка, которая открывает подменю или категорию  
**Тип клавиатуры:** InlineKeyboardMarkup (кнопки под сообщением)  
**Использование:** Для навигации по меню, открытия категорий

**Пример в таблице:**
| ID | Название | Родитель ID | Тип | Текст | Порядок |
|----|----------|-------------|-----|-------|---------|
| 1 | Услуги | | mess_button | | 1 |

### 2. `message`
**Описание:** Текстовое сообщение без подменю  
**Тип клавиатуры:** InlineKeyboardMarkup (кнопка для возврата)  
**Использование:** Для отображения информации, текста

**Пример в таблице:**
| ID | Название | Родитель ID | Тип | Текст | Порядок |
|----|----------|-------------|-----|-------|---------|
| 10 | О нас | 1 | message | Мы лучшая компания... | 1 |

### 3. `reply_button`
**Описание:** Кнопка внизу экрана (Reply клавиатура), по одной в ряд  
**Тип клавиатуры:** ReplyKeyboardMarkup (кнопки внизу экрана)  
**Использование:** Для быстрого доступа к основным функциям

**Пример в таблице:**
| ID | Название | Родитель ID | Тип | Текст | Порядок |
|----|----------|-------------|-----|-------|---------|
| 20 | Главное меню | | reply_button | | 1 |
| 21 | Помощь | | reply_button | | 2 |

### 4. `reply_button_line` (новый тип)
**Описание:** Кнопка внизу экрана, несколько кнопок в один ряд (горизонтально)  
**Тип клавиатуры:** ReplyKeyboardMarkup (кнопки внизу экрана)  
**Использование:** Для группировки связанных действий в один ряд

**Пример в таблице:**
| ID | Название | Родитель ID | Тип | Текст | Порядок |
|----|----------|-------------|-----|-------|---------|
| 22 | Купить | | reply_button_line | | 1 |
| 23 | Продать | | reply_button_line | | 2 |
| 24 | Обмен | | reply_button_line | | 3 |

**Результат:** Все три кнопки будут в один ряд: `[Купить] [Продать] [Обмен]`

## 🔄 Миграция с `button` на `mess_button`

Старый тип `button` автоматически конвертируется в `mess_button` для обратной совместимости.

**Рекомендация:** Обновите все `button` на `mess_button` в вашей таблице.

## 💡 Когда использовать каждый тип

### Используйте `mess_button`, когда:
- Нужна навигация по меню
- Нужно открыть категорию с подпунктами
- Кнопка должна быть под сообщением (inline)

### Используйте `message`, когда:
- Нужно показать текст/информацию
- Не требуется подменю
- Кнопка только для возврата назад

### Используйте `reply_button`, когда:
- Нужен быстрый доступ к основным функциям
- Кнопка должна быть всегда видна внизу экрана
- Нужна постоянная навигация
- Кнопка должна быть отдельно (по одной в ряд)

### Используйте `reply_button_line`, когда:
- Нужно сгруппировать несколько связанных действий
- Кнопки должны быть в один ряд (горизонтально)
- Это варианты выбора или связанные действия (Да/Нет, Купить/Продать)

## 📝 Пример структуры меню

```
Главное меню (mess_button)
├── Услуги (mess_button)
│   ├── Обмен валют (message)
│   └── Консультация (message)
├── О нас (message)
└── Контакты (message)

Reply кнопки (внизу экрана):
├── Главное меню (reply_button)        ← одна кнопка
├── Помощь (reply_button)              ← одна кнопка
├── Купить (reply_button_line)         \
├── Продать (reply_button_line)        | → в один ряд
└── Обмен (reply_button_line)          /
```

## 🎨 Визуальное отличие

- **mess_button** и **message**: Кнопки под сообщением (inline)
- **reply_button**: Кнопки внизу экрана, всегда видимы

## ⚙️ Технические детали

### Inline кнопки (mess_button, message)
- Отправляются через `InlineKeyboardMarkup`
- Появляются под сообщением
- Исчезают при нажатии (если настроено)

### Reply кнопки (reply_button)
- Отправляются через `ReplyKeyboardMarkup`
- Появляются внизу экрана
- Остаются видимыми до замены или удаления
- Обрабатываются через текстовые сообщения

## 🔧 Настройка в Google Sheets

В столбце "Тип" укажите:
- `mess_button` - для inline кнопок с подменю
- `message` - для текстовых сообщений
- `reply_button` - для кнопок внизу экрана (по одной в ряд)
- `reply_button_line` - для кнопок внизу экрана (несколько в один ряд)

## ⚠️ Важно

1. **reply_button** обрабатываются по тексту кнопки, поэтому названия должны быть уникальными
2. **reply_button** могут иметь дочерние элементы (mess_button или message)
3. При нажатии на **reply_button** бот ищет элемент с таким же названием и типом `reply_button`
