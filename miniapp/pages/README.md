# Pages Structure

Профессиональная структура страниц для Mini App.

## 📁 Структура

```
pages/
├── exchange.html          # HTML страницы обмена
├── support.html           # HTML страницы поддержки
├── about.html             # HTML страницы "О нас"
├── requests.html          # HTML страницы заявок
├── css/
│   ├── exchange.css       # Стили страницы обмена
│   ├── support.css        # Стили страницы поддержки
│   ├── about.css          # Стили страницы "О нас"
│   └── requests.css       # Стили страницы заявок
├── js/
│   ├── exchange.js        # Логика страницы обмена
│   ├── support.js         # Логика страницы поддержки
│   ├── about.js           # Логика страницы "О нас"
│   └── requests.js        # Логика страницы заявок
└── README.md              # Документация
```

## 🔄 Динамическая загрузка

Страницы загружаются динамически при первом обращении:

1. **HTML** - загружается из `pages/exchange.html`
2. **CSS** - загружается из `pages/css/exchange.css`
3. **JS** - загружается из `pages/js/exchange.js`

## 📝 Добавление новой страницы

1. Создайте HTML файл: `pages/[name].html`
2. Создайте CSS файл: `pages/css/[name].css`
3. Создайте JS файл: `pages/js/[name].js`
4. Добавьте функцию загрузки в `app.js`:

```javascript
async function load[name]Page() {
    // Загрузка HTML, CSS, JS
    // Инициализация страницы
}
```

## 🎯 Текущие страницы

- **exchange** - Страница обмена (купить/продать USDT)
- **support** - Страница поддержки (FAQ + контакт)
- **about** - Страница "О нас" (статистика + преимущества)
- **requests** - Страница заявок (история + фильтрация)
