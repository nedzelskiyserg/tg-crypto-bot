# ===========================================
# TG CRYPTO BOT — переменные окружения
# Скопируйте в .env и заполните значения
# ===========================================

# Telegram Bot (обязательно)
BOT_TOKEN=your_bot_token_from_botfather

# База данных (по умолчанию SQLite в backend/data/app.db)
# DATABASE_URL=sqlite+aiosqlite:///./data/app.db

# Файл с ID админов (Excel). По умолчанию: backend/admins.xlsx
# ADMINS_FILE=/app/backend/admins.xlsx

# CORS: разрешённые источники для API (через запятую).
# В продакшене укажите домен Mini App, например: https://your-domain.com
# Пусто или не задано = разрешены все (*)
# CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Публичный URL приложения (опционально, для логов/уведомлений)
# PUBLIC_URL=https://your-domain.com
