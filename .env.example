# ===========================================
# TG CRYPTO BOT — переменные окружения
# Один файл для инлайн-бота и Mini App (TMA).
# Сгенерировать .env: ./scripts/generate_env.sh
# ===========================================

# Telegram Bot (обязательно — подставьте свой токен от BotFather)
BOT_TOKEN=your_bot_token_from_botfather

# Google таблица (инлайн-бот: меню; TMA: админы с листа Settings, ключ admins/админы)
USE_GOOGLE_SHEETS=true
GOOGLE_SHEETS_ID=1OVEn5gfgvngL8nBk5iYAHk8AtQN5FgYA4Hxp_l_FKdo
GOOGLE_CREDENTIALS_PATH=credentials.json

# База данных TMA (по умолчанию SQLite в data/app.db)
# DATABASE_URL=sqlite+aiosqlite:///./data/app.db

# Админы TMA: из Google (лист Settings) или из Excel, если Google не задан
# ADMINS_FILE=backend/admins.xlsx

# CORS: домен Mini App в продакшене (через запятую). Пусто = разрешены все
# CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Публичный URL (опционально)
# PUBLIC_URL=https://your-domain.com
