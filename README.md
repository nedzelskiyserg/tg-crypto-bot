# Telegram Bot & Mini App для Mosca

Профессиональная инфраструктура для Telegram бота и TMA Mini App с динамическим меню из Google Sheets.

## 📁 Структура проекта

```
.
├── bot/                    # Telegram бот
│   ├── handlers/           # Обработчики команд и сообщений
│   ├── cms/                # Система управления контентом
│   ├── keyboards/           # Построение клавиатур
│   ├── utils/              # Утилиты
│   └── main.py             # Точка входа бота
├── miniapp/                # TMA Mini App (будущее)
├── shared/                 # Общие модули
│   └── config.py           # Конфигурация
├── scripts/                # Вспомогательные скрипты
├── docs/                   # Документация
├── tests/                  # Тесты
├── .env.example            # Пример конфигурации
├── Dockerfile              # Docker образ для бота
├── docker-compose.yml      # Docker Compose конфигурация
└── requirements.txt        # Python зависимости
```

## 🚀 Быстрый старт

### Локальная разработка

1. Клонируйте репозиторий
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
3. Скопируйте `.env.example` в `.env` и заполните:
   ```bash
   cp .env.example .env
   ```
4. Запустите бота:
   ```bash
   python -m bot.main
   ```

### Docker

1. Скопируйте `.env.example` в `.env` и заполните
2. Запустите:
   ```bash
   docker-compose up -d
   ```

## 📚 Документация

Подробная документация находится в папке `docs/`:

- `QUICK_START.md` - Быстрый старт
- `CMS_INSTRUCTIONS.md` - Инструкции по работе с CMS
- `GOOGLE_SHEETS_SETUP.md` - Настройка Google Sheets
- `BUTTON_TYPES_GUIDE.md` - Типы кнопок
- И другие...

## 🔧 Конфигурация

Все настройки находятся в файле `.env`. См. `.env.example` для примера.

## 📦 Деплой

Проект готов к деплою на сервер:

1. Используйте Docker Compose для простого деплоя
2. Или запустите напрямую: `python -m bot.main`

## 🛠 Разработка

### Структура модулей

- `bot/` - код Telegram бота
- `miniapp/` - код Mini App (будет добавлен)
- `shared/` - общие модули для бота и Mini App

### Добавление новых функций

1. Обработчики бота: `bot/handlers/`
2. CMS модули: `bot/cms/`
3. Клавиатуры: `bot/keyboards/`

## 📝 Лицензия

Проект для внутреннего использования.
