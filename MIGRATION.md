# Миграция на новую структуру проекта

Проект был реорганизован для профессионального деплоя и поддержки двух сущностей: Telegram бота и TMA Mini App.

## 🔄 Что изменилось

### Структура проекта

**Было:**
```
.
├── main.py
├── cms.py
├── google_sheets_cms.py
├── keyboards.py
└── ...
```

**Стало:**
```
.
├── bot/                    # Telegram бот
│   ├── handlers/           # Обработчики
│   ├── cms/                # CMS модули
│   ├── keyboards/           # Клавиатуры
│   ├── utils/              # Утилиты
│   └── main.py             # Точка входа
├── miniapp/                # TMA Mini App (будущее)
├── shared/                 # Общие модули
├── scripts/                # Скрипты
└── docs/                   # Документация
```

### Запуск бота

**Было:**
```bash
python3 main.py
```

**Стало:**
```bash
python3 -m bot.main
```

### Импорты

Все импорты обновлены для новой структуры. Старые файлы (`main.py`, `cms.py`, `google_sheets_cms.py`, `keyboards.py`) сохранены как резервные копии, но не используются.

## ✅ Что нужно сделать

1. **Обновить команды запуска:**
   - В скриптах автоматизации
   - В systemd сервисах
   - В Docker Compose (уже обновлено)

2. **Проверить работу:**
   ```bash
   python3 -m bot.main
   ```

3. **Обновить документацию:**
   - Все инструкции в `docs/` уже обновлены

## 📝 Примечания

- Старые файлы (`main.py`, `cms.py`, и т.д.) можно удалить после проверки работы новой структуры
- Все функции сохранены и работают так же, как раньше
- Структура готова к добавлению Mini App в будущем
